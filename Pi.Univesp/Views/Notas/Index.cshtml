@model IEnumerable<Pi.Univesp.Models.Nota>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Criar novo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ValorNota)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Data)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Disciplina)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ValorNota)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Data)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Disciplina.DisciplinaId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.NotaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.NotaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.NotaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
